interface Person {
    name : string,
};

interface Person {
     age : number
};

interface Person {
     city : string
};




const p : Person  = {
     name : "sharon",
     age : 24,
     city : "kochi",
};


console.log(p)
