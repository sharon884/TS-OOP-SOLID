function accept ( value : string | null ) : void {
   
     if ( value == null ) {
            console.log(0);
     }else {
      console.log(value.length);
     }
};


accept(null);
accept("sharon");
 